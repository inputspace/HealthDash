#!/bin/sh
GRADLE_OPTS="${GRADLE_OPTS:-""}" exec "$(dirname "$0")/gradle/wrapper/gradle-wrapper.jar" "$@"
#!/usr/bin/env sh
APP_HOME="$(cd "$(dirname "$0")" && pwd)"
exec java -classpath "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" org.gradle.wrapper.GradleWrapperMain "$@"
